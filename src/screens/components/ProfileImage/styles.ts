import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  profileImageContainer: {
    alignItems: "center",
    margin: 25,
  },
  profileImage: {
    width: 100,
    height: 100,
    borderRadius: 50,
  },
});
